const $=s=>document.querySelector(s);const screens=['home','create','result','battle'];let monster=null;
const emojis=['🦝','🦈','🐸','🦆','🦑','🐲','🦄','🍌','🧃','🦖','🐙','🥔'];const traits=['SUPREMO','NINJA','DESTROYER','GOBLIN','WARRIOR','BOSS','PROPHET','MENACE'];const lore=['escaped from a suspicious supermarket at 3AM.','was banned from three dimensions for excessive aura.','has one mission: become ridiculously famous.','looks harmless. It is absolutely not.','was created by accident and refuses to apologize.'];
function show(id){screens.forEach(x=>$('#'+x).classList.toggle('active',x===id));scrollTo(0,0)}
function hash(s){let h=2166136261;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return Math.abs(h)}
function makeMonster(raw){const h=hash(raw);const stats=[55+h%46,50+(h>>3)%51,45+(h>>6)%56,55+(h>>9)%46];const power=Math.round(stats.reduce((a,b)=>a+b,0)/4);return{name:(['TURBO','LORD','CAPTAIN','PROFESSOR','KING','GENERAL'][h%6]+' '+traits[Math.floor(h/11)%traits.length]),emoji:emojis[h%emojis.length],lore:lore[h%lore.length],stats,rarity:power>90?'MYTHIC':power>78?'LEGENDARY':power>65?'EPIC':'UNCOMMON',power,seed:h,raw}}
function render(m){monster=m;$('#monsterEmoji').textContent=m.emoji;$('#monsterName').textContent=m.name;$('#monsterLore').textContent=m.lore;$('#rarity').textContent=m.rarity;['power','chaos','luck','aura'].forEach((x,i)=>$('#'+x).textContent=i===0?m.power:m.stats[i]);$('#battleEmoji').textContent=m.emoji;$('#battleName').textContent=m.name;$('#battlePower').textContent='POWER '+m.power}
function generate(){const raw=$('#nameInput').value.trim()||'Anonymous';render(makeMonster(raw));show('result')}
$('#createBtn').onclick=()=>show('create');$('#generateBtn').onclick=generate;document.querySelectorAll('[data-home]').forEach(b=>b.onclick=()=>show('home'));
function encode(m){return btoa(unescape(encodeURIComponent(JSON.stringify(m)))).replace(/=/g,'')}
function challengeUrl(m){return location.href.split('#')[0]+'#challenge='+encode(m)}
$('#challengeBtn').onclick=()=>{if(!monster)return;const url=challengeUrl(monster);$('#shareUrl').textContent=url;show('battle')}
$('#shareBtn').onclick=async()=>{if(!monster)return;const url=challengeUrl(monster);const text=`I created ${monster.name} ${monster.emoji} — POWER ${monster.power}. Can you beat my monster?`;if(navigator.share){try{await navigator.share({title:'WHO ARE YOU?',text,url})}catch{}}else{await navigator.clipboard?.writeText(text+' '+url);$('#shareBtn').textContent='COPIED ✓';setTimeout(()=>$('#shareBtn').textContent='↗ SHARE MY MONSTER',1600)}};
$('#copyBtn').onclick=async()=>{await navigator.clipboard?.writeText($('#shareUrl').textContent);$('#copyBtn').textContent='COPIED ✓';setTimeout(()=>$('#copyBtn').textContent='COPY CHALLENGE LINK 🔗',1600)};
function decode(v){return JSON.parse(decodeURIComponent(escape(atob(v))))}
const params=location.hash;if(params.startsWith('#challenge=')){try{render(decode(params.slice(11)));setTimeout(()=>show('result'),50)}catch{}}
