const SUPABASE_URL='https://pbouvkzalxeragrpsotl.supabase.co';const SUPABASE_KEY=window.SUPABASE_PUBLISHABLE_KEY||'';const $=s=>document.querySelector(s);const screens=['home','create','challenge','result','battle','fusion','leaderboard','collection'];let monster=null,room=null,channel=null,incoming=null;const emojis=['🦝','🦈','🐸','🦆','🦑','🐲','🦄','🍌','🧃','🦖','🐙','🥔','👽','🧠','🦀','🌚'];const traits=['SUPREMO','NINJA','DESTROYER','GOBLIN','WARRIOR','BOSS','PROPHET','MENACE','OVERLORD','CHAOS'];const lore=['escaped from a suspicious supermarket at 3AM.','was banned from three dimensions for excessive aura.','has one mission: become ridiculously famous.','looks harmless. It is absolutely not.','was created by accident and refuses to apologize.','has never lost an argument with a vending machine.'];const playerId=localStorage.getItem('who_player_id')||crypto.randomUUID();localStorage.setItem('who_player_id',playerId);function show(id){screens.forEach(x=>$('#'+x)?.classList.toggle('active',x===id));scrollTo(0,0);if(id==='leaderboard')loadLeaderboard('all');if(id==='home')loadLeaderboard('daily','leaderboardHome');if(id==='collection')loadCollection()}function hash(s){let h=2166136261;for(let i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619)}return Math.abs(h)}function todaySeed(){const d=new Date();return d.getUTCFullYear()+'-'+String(d.getUTCMonth()+1).padStart(2,'0')+'-'+String(d.getUTCDate()).padStart(2,'0')}function makeMonster(raw,salt=''){const h=hash(raw+salt),stats=[55+h%46,50+(h>>3)%51,45+(h>>6)%56,55+(h>>9)%46],power=Math.round(stats.reduce((a,b)=>a+b,0)/4);return{name:(['TURBO','LORD','CAPTAIN','PROFESSOR','KING','GENERAL'][h%6]+' '+traits[Math.floor(h/11)%traits.length]),emoji:emojis[h%emojis.length],lore:lore[h%lore.length],stats,rarity:power>90?'MYTHIC':power>78?'LEGENDARY':power>65?'EPIC':'UNCOMMON',power,seed:h,raw}}function getStreak(){return Number(localStorage.getItem('who_streak')||0)}function recordWin(){const day=todaySeed(),last=localStorage.getItem('who_last_win');let streak=getStreak();if(last===day)return streak;const d=new Date();d.setUTCDate(d.getUTCDate()-1);const prev=d.toISOString().slice(0,10);streak=last===prev?streak+1:1;localStorage.setItem('who_streak',streak);localStorage.setItem('who_last_win',day);return streak}function render(m){monster=m;$('#monsterEmoji').textContent=m.emoji;$('#monsterName').textContent=m.name;$('#monsterLore').textContent=m.lore;$('#rarity').textContent=m.rarity;['power','chaos','luck','aura'].forEach((x,i)=>$('#'+x).textContent=i===0?m.power:m.stats[i]);$('#streakValue').textContent=getStreak()}function supa(){if(!SUPABASE_KEY)throw Error('Missing publishable key');if(!window._supa)window._supa=window.supabase.createClient(SUPABASE_URL,SUPABASE_KEY);return window._supa}function encode(m){return btoa(unescape(encodeURIComponent(JSON.stringify(m)))).replace(/=/g,'')}function decode(v){return JSON.parse(decodeURIComponent(escape(atob(v))))}function baseUrl(){return location.href.split('#')[0]}function challengeUrl(m,code=''){return baseUrl()+'#challenge='+encode({...m,roomCode:code})}function shareText(text,url=challengeUrl(monster)){if(navigator.share)navigator.share({title:'WHO ARE YOU?',text,url}).catch(()=>{});else navigator.clipboard?.writeText(text+' '+url)}function saveCollection(m){const a=JSON.parse(localStorage.getItem('who_collection')||'[]');if(!a.some(x=>x.seed===m.seed))a.unshift(m);localStorage.setItem('who_collection',JSON.stringify(a.slice(0,50)))}function loadCollection(){const el=$('#collectionList');if(!el)return;const a=JSON.parse(localStorage.getItem('who_collection')||'[]');el.innerHTML=a.length?a.map(x=>`<div class="rank"><b>${x.emoji}</b><span>${x.name}</span><small>⚡ ${x.power} · ${x.rarity}</small></div>`).join(''):'No monsters saved yet.'}async function createRoom(){try{const code=Math.random().toString(36).slice(2,8).toUpperCase();const{data,error}=await supa().from('game_battles').insert({code,creator:monster,creator_id:playerId,status:'waiting',day:todaySeed()}).select().single();if(error)throw error;room=data;$('#shareUrl').textContent=challengeUrl(monster,code);$('#battleStatus').textContent='WAITING FOR YOUR FRIEND…';show('battle');subscribeRoom(code)}catch(e){alert('Battle needs the Supabase publishable key to connect.')}}function subscribeRoom(code){if(channel)supa().removeChannel(channel);channel=supa().channel('game:'+code).on('postgres_changes',{event:'UPDATE',schema:'public',table:'game_battles',filter:'code=eq.'+code},p=>{room=p.new;if(room.opponent)renderFight(room)}).subscribe()}async function joinRoom(code){try{const{data,error}=await supa().from('game_battles').select('*').eq('code',code).single();if(error||!data||data.status==='finished'||data.opponent)throw Error('full');room=data;const mine=monster||makeMonster($('#nameInput').value.trim()||'Challenger',String(Date.now()));const{data:updated,error:up}=await supa().from('game_battles').update({opponent:mine,opponent_id:playerId,status:'ready'}).eq('code',code).select().single();if(up)throw up;room=updated;show('battle');renderFight(room);subscribeRoom(code)}catch(e){alert('This battle is unavailable or already full.')}}function battle(a,b){const A=a.power+a.seed%17,B=b.power+b.seed%17;return A>=B?{winner:a,loser:b,margin:A-B}:{winner:b,loser:a,margin:B-A}}async function finishBattle(){if(!room?.code||room.status==='finished')return;const{data,error}=await supa().from('game_battles').update({status:'finished'}).eq('code',room.code).select().single();if(error)return alert('Could not save the battle.');room=data;const winner=data.winner;if(winner?.name===monster?.name){const streak=recordWin();await supa().from('game_player_stats').upsert({player_id:playerId,streak,best_streak:streak,last_win_date:todaySeed(),wins:1,highest_power:monster.power,updated_at:new Date().toISOString()},{onConflict:'player_id'});$('#streakValue').textContent=streak}renderFight(data)}function renderFight(r){if(!r?.creator||!r?.opponent)return;$('#battleLeft').textContent=r.creator.emoji;$('#battleLeftName').textContent=r.creator.name;$('#battleLeftPower').textContent='POWER '+r.creator.power;$('#battleRight').textContent=r.opponent.emoji;$('#battleRightName').textContent=r.opponent.name;$('#battleRightPower').textContent='POWER '+r.opponent.power;const result=r.winner?{winner:r.winner,loser:r.winner.name===r.creator.name?r.opponent:r.creator,margin:Math.abs(r.winner.power-(r.winner.name===r.creator.name?r.opponent:r.creator).power)}:battle(r.creator,r.opponent);$('#battleStatus').textContent=r.status==='finished'?'BATTLE COMPLETE':'LIVE BATTLE';$('#battleResult').innerHTML=`<div class="winner">${result.winner.emoji}</div><div class="win-title">${result.winner.name} WINS</div><p>${result.winner.power} power vs ${result.loser.power} · victory margin ${result.margin}</p><div class="streak-box">🔥 WIN STREAK <b>${monster?.name===result.winner.name?getStreak():0}</b></div>${r.status==='finished'?'':'<button class="primary wide" id="finishBtn">🏆 SAVE RESULT</button>'}<button class="secondary wide" id="shareResultBtn">↗ SHARE RESULT</button><button class="secondary wide" id="fusionBtn">🧬 FUSE THESE MONSTERS</button>`;if($('#finishBtn'))$('#finishBtn').onclick=finishBattle;$('#shareResultBtn').onclick=()=>shareText(`🏆 ${result.winner.name} won with POWER ${result.winner.power}.`,challengeUrl(result.winner,r.code));$('#fusionBtn').onclick=()=>fuse(r.creator,r.opponent);show('battle')}function fuse(a,b){const seed=hash(a.name+b.name),f=makeMonster(a.name.slice(0,7)+b.name.slice(0,7),String(seed));f.name=a.name.split(' ')[0]+' × '+b.name.split(' ')[0];f.emoji=emojis[seed%emojis.length]+' '+emojis[(seed>>4)%emojis.length];f.power=Math.min(100,Math.round((a.power+b.power)/2+5));f.rarity=f.power>92?'MYTHIC FUSION':f.power>80?'LEGENDARY FUSION':'EPIC FUSION';$('#fusionEmoji').textContent=f.emoji;$('#fusionName').textContent=f.name;$('#fusionPower').textContent='POWER '+f.power;$('#fusionRarity').textContent=f.rarity;monster=f;saveCollection(f);show('fusion')}async function loadLeaderboard(mode='all',target='leaderboardList'){const el=$('#'+target);if(!el)return;try{let q=supa().from('game_battles').select('winner,finished_at').eq('status','finished').order('finished_at',{ascending:false}).limit(500);if(mode==='daily')q=q.gte('finished_at',todaySeed()+'T00:00:00Z');const{data,error}=await q;if(error)throw error;const map={};(data||[]).forEach(b=>{if(!b.winner)return;const w=b.winner,n=w.name||'UNKNOWN';map[n]??={name:n,emoji:w.emoji||'👾',wins:0,power:0};map[n].wins++;map[n].power=Math.max(map[n].power,w.power||0)});const rows=Object.values(map).sort((a,b)=>b.wins-a.wins||b.power-a.power).slice(0,10);el.innerHTML=rows.length?rows.map((x,i)=>`<div class="rank"><b>#${i+1} ${x.emoji}</b><span>${x.name}</span><small>🏆 ${x.wins} · ⚡ ${x.power}</small></div>`).join(''):'No battles yet — be the first.'}catch(e){el.textContent='Connect Supabase to load the leaderboard.'}}function openChallenge(m){incoming=m;$('#challengeEmoji').textContent=m.emoji;$('#challengeName').textContent=m.name;$('#challengeLore').textContent=m.lore;$('#challengePower').textContent=m.power;$('#challengeRarity').textContent=m.rarity;show('challenge')}$('#createBtn').onclick=()=>show('create');$('#generateBtn').onclick=async()=>{const raw=$('#nameInput').value.trim()||'Anonymous';render(makeMonster(raw));saveCollection(monster);const code=$('#generateBtn').dataset.room;show('result');if(code)await joinRoom(code)};document.querySelectorAll('[data-home]').forEach(b=>b.onclick=()=>show('home'));$('#challengeBtn').onclick=createRoom;$('#shareBtn').onclick=()=>shareText(`I created ${monster.name} ${monster.emoji} — POWER ${monster.power}. Can you beat my monster?`);$('#collectionBtn').onclick=()=>{saveCollection(monster);show('collection')};$('#copyBtn').onclick=async()=>{await navigator.clipboard?.writeText($('#shareUrl').textContent);$('#copyBtn').textContent='COPIED ✓';setTimeout(()=>$('#copyBtn').textContent='COPY BATTLE LINK 🔗',1600)};$('#shareFusionBtn').onclick=()=>shareText(`I fused ${monster.name} 🧬 POWER ${monster.power}`);$('#openLeaderboard').onclick=()=>show('leaderboard');$('#dailyTab').onclick=()=>loadLeaderboard('daily');$('#allTab').onclick=()=>loadLeaderboard('all');$('#acceptChallengeBtn').onclick=()=>{show('create');$('#generateBtn').dataset.room=incoming?.roomCode||''};$('#challengeShareBtn').onclick=()=>shareText(`⚔️ Can you beat ${incoming?.name}? POWER ${incoming?.power}`,challengeUrl(incoming,incoming?.roomCode||''));const params=location.hash;if(params.startsWith('#room=')){setTimeout(()=>{show('create');$('#nameInput').focus();$('#generateBtn').dataset.room=params.slice(6)},100)}else if(params.startsWith('#challenge=')){try{openChallenge(decode(params.slice(11)))}catch{}}else setTimeout(()=>loadLeaderboard('daily','leaderboardHome'),200)